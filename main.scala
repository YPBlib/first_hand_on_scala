assert(true,"test")
var rs=Range('a','z')
rs= rs.reverse




class Counter{
    var i=0
    def gen():Int = i+1
}

// import util._
import util.Random
val r= new Random
println(r.nextInt(r.nextInt(r.nextInt(31))))

import util.Properties
val p=Properties

for(i <- 0 to 2){
    for(j <- 0 until 3){
        for(k <- Range(0,3)){
            println(i,j,k)
        }
    }
}

val vi= Vector(1,2,3,4,5)

